# The provider self-generates a TLS secret (self-signed CA + server cert) for
# the instance. Assert it exists with the expected kubernetes.io/tls shape; the
# CA contents are validated by the chainsaw script step.
apiVersion: v1
kind: Secret
metadata:
  name: test-valkey-cluster-tls
type: kubernetes.io/tls
