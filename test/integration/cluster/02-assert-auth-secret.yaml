# The provider self-generates an auth secret holding the default user's
# password. Assert it exists with the expected key so the ValkeyCluster user
# reference resolves.
apiVersion: v1
kind: Secret
metadata:
  name: test-valkey-cluster-auth
type: Opaque
