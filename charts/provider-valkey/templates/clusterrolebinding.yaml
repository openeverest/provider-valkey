{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "provider-valkey.fullname" . }}
  labels:
    {{- include "provider-valkey.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "provider-valkey.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "provider-valkey.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
