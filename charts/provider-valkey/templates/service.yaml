apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-valkey.fullname" . }}
  labels:
    {{- include "provider-valkey.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-valkey.selectorLabels" . | nindent 4 }}
